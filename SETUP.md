# 하루 — 처음부터 끝까지 설치하기

아이폰에서 앱처럼 쓰고, 앱을 닫아도 알림을 받는 것까지 한 번에 끝내는 순서입니다.
각 단계마다 **[컴퓨터]** 또는 **[아이폰]** 을 적어뒀어요. 그 기기에서 하셔야 합니다.

전체 1~2단계까지만 해도 앱은 다 씁니다. 알림이 필요하면 3단계부터 이어서 하세요.
돈은 한 푼도 들지 않습니다. 신용카드도 필요 없습니다.

---

## 0단계 [아이폰 또는 컴퓨터] 파일 받기

1. 이 대화에서 `haru-pwa.zip` 을 누르고 **다운로드**
2. 압축 풀기
   - 컴퓨터: 파일을 두 번 누르면 `haru` 폴더가 생깁니다
   - 아이폰: 파일 앱 → 다운로드 → `haru-pwa.zip` 을 한 번 누르면 폴더로 풀립니다
3. 폴더 안에 파일 10개가 있는지 확인

| 파일 | 역할 |
|---|---|
| `index.html` | 앱 본체 |
| `manifest.webmanifest` | 앱 이름·아이콘 설정 |
| `sw.js` | 오프라인 실행 + 알림 수신 |
| `keygen.html` | 알림 열쇠 만드는 페이지 (3단계에서 사용) |
| `worker.js` | 알림 서버 코드 (5단계에서 붙여넣기, 올려도 되고 안 올려도 됨) |
| `icon-192.png` `icon-512.png` `icon-maskable.png` `apple-touch-icon.png` | 아이콘 |
| `README.md` `SETUP.md` | 안내 문서 |

파일 이름은 절대 바꾸지 마세요. 서로를 이름으로 찾습니다.

---

## 1단계 [컴퓨터] 앱을 인터넷에 올리기

### 방법 A — Netlify (제일 쉬움, 5분, 가입 없이도 됨)

1. 브라우저로 **https://app.netlify.com/drop** 접속
2. `haru` 폴더를 페이지 위로 **끌어다 놓기** (폴더째로 올려야 합니다)
3. 잠시 뒤 `https://무작위이름.netlify.app` 주소가 나옵니다 ← **이게 앱 주소입니다. 메모해 두세요**
4. 무료 가입을 하면 Site configuration → Change site name 에서 주소를 `haru-내이름.netlify.app` 으로 바꾸고 계속 보관할 수 있습니다

### 방법 B — GitHub Pages (아이폰만 있어도 가능, 10분)

1. 사파리로 **https://github.com** → 무료 가입 → 로그인
2. 오른쪽 위 `+` → **New repository** → Repository name 에 `haru` → **Public** 선택 → **Create repository**
3. 다음 화면에서 **uploading an existing file** 링크 누르기
4. **choose your files** → 압축 푼 폴더에서 **파일 전체를 선택** (폴더가 아니라 파일들을 골라야 합니다)
5. 맨 아래 **Commit changes** 누르기
6. 위쪽 **Settings** → 왼쪽 메뉴에서 **Pages** → Branch 를 `main`, 폴더는 `/ (root)` → **Save**
7. 1~2분 기다린 뒤 그 페이지를 새로고침하면 `https://내아이디.github.io/haru/` 주소가 나옵니다 ← **이게 앱 주소입니다**

> 반드시 `https://` 로 시작하는 주소여야 합니다. 파일을 그냥 두 번 눌러서 여는 방식(`file://`)은 설치도 알림도 안 됩니다.

---

## 2단계 [아이폰] 홈 화면에 추가 — 꼭 하세요

1. **사파리로** 1단계 주소를 엽니다. 크롬이 아니라 사파리여야 합니다
2. 화면 아래 공유 버튼(↑) 누르기
3. 목록을 내려서 **홈 화면에 추가** → 오른쪽 위 **추가**
4. 이제부터는 **홈 화면 아이콘으로만** 여세요

홈 화면 아이콘으로 여는 게 중요한 이유

- 아이폰은 사파리 탭으로만 쓰는 사이트의 저장 기록을 며칠 뒤 지울 수 있습니다. 홈 화면 앱은 지우지 않습니다
- **알림 권한은 홈 화면 앱에서만** 받을 수 있습니다
- 사파리 탭과 홈 화면 앱은 저장 공간이 따로입니다. 사파리에서 미리 적어둔 게 있으면 통계 탭 → **백업 파일 내려받기** → 홈 화면 앱에서 **백업 파일 불러오기**로 옮기세요

여기까지가 앱 설치 끝입니다. 알림이 필요 없으면 그만해도 됩니다.

---

## 3단계 [컴퓨터] 알림 열쇠 만들기

서버와 아이폰이 서로를 알아보는 열쇠 한 쌍을 만듭니다.

1. 1단계 주소 뒤에 `/keygen.html` 을 붙여서 접속
   예: `https://haru-내이름.netlify.app/keygen.html`
2. **열쇠 만들기** 버튼 누르기
3. 두 개의 값이 나옵니다. **둘 다 메모 앱에 복사해 두세요**
   - 공개 열쇠 (짧은 한 줄)
   - 비밀 열쇠 (`{"d":"...","x":"...","y":"..."}` 모양의 한 줄)

이 페이지를 닫으면 같은 열쇠를 다시 볼 수 없습니다. 반드시 먼저 복사하세요.
비밀 열쇠는 남에게 보여주지 마세요.

---

## 4단계 [컴퓨터] 클라우드플레어 가입하고 저장소 만들기

1. **https://dash.cloudflare.com/sign-up** 에서 이메일로 무료 가입 (카드 필요 없음)
2. 로그인하면 왼쪽 메뉴에서 **Storage & Databases → KV** (또는 Workers & Pages → KV)
3. **Create a namespace** → 이름에 `haru-store` → 만들기

이 KV가 "어느 폰에 언제 알림을 보낼지" 적어두는 수첩입니다.

---

## 5단계 [컴퓨터] 알림 서버(워커) 만들기

1. 왼쪽 메뉴 **Workers & Pages** → **Create** → **Workers** → **Create Worker**
2. 이름을 `haru-alarm` 으로 하고 **Deploy** 누르기 (기본 코드로 일단 배포됩니다)
3. **Edit code** (또는 연필 아이콘) 누르기
4. 편집기에 있던 코드를 **전부 지우고**, 받은 폴더의 `worker.js` 파일을 텍스트 편집기로 열어 **내용 전체를 복사해서 붙여넣기**
5. 오른쪽 위 **Deploy** 누르기

그러면 `https://haru-alarm.내계정이름.workers.dev` 같은 주소가 생깁니다. ← **이게 알림 서버 주소입니다. 메모해 두세요**

---

## 6단계 [컴퓨터] 워커에 열쇠·저장소·시계 연결하기

워커 화면에서 **Settings** 로 들어갑니다. 세 가지를 설정합니다.

### 6-1. 저장소 연결

- **Bindings** (또는 Variables and Bindings) → **Add binding** → **KV Namespace**
- Variable name 칸에 정확히 **`HARU`** (대문자)
- KV namespace 는 4단계에서 만든 `haru-store` 선택 → **Deploy / Save**

> 변수 이름을 `HARU` 이외로 쓰면 작동하지 않습니다.

### 6-2. 열쇠 넣기

같은 화면의 **Variables and Secrets** 에서 **Add** 를 눌러 세 개를 넣습니다.

| 이름 | 값 | 종류 |
|---|---|---|
| `VAPID_PUBLIC` | 3단계의 공개 열쇠 | Text |
| `VAPID_JWK` | 3단계의 비밀 열쇠 (중괄호까지 전부) | Secret |
| `VAPID_SUBJECT` | `mailto:내이메일@example.com` | Text |

`VAPID_SUBJECT` 는 반드시 `mailto:` 로 시작해야 합니다. 애플이 그걸 요구합니다.
넣고 나서 **Deploy** 를 눌러 저장합니다.

### 6-3. 시계 걸기

- **Settings → Triggers → Cron Triggers** → **Add Cron Trigger**
- 칸에 별 다섯 개와 공백: `* * * * *`
- 저장

이제 서버가 1분마다 "지금 울릴 알림이 있나?" 확인합니다.

### 6-4. 확인

브라우저에서 알림 서버 주소 뒤에 `/health` 를 붙여 접속하세요.
예: `https://haru-alarm.내계정.workers.dev/health`

`"ok": true` 가 보이면 성공입니다. `false` 면 어느 항목이 `false` 인지 보고 그 부분을 다시 설정하세요.

---

## 7단계 [아이폰] 앱과 알림 서버 연결하기

**홈 화면 아이콘으로 앱을 열고** 진행하세요.

1. 아래 탭에서 **루틴** → 맨 아래 **폰 알림 (앱을 닫아도 옴)** 카드
2. **＋ 폰 알림 연결하기** 누르기
3. 칸에 6단계의 알림 서버 주소 붙여넣기 (`https://haru-alarm...workers.dev`)
4. **주소 저장하고 연결하기** 누르기
5. 아이폰이 알림 권한을 물어보면 **허용**
6. **테스트 알림 보내기** 를 누르면 몇 초 안에 알림이 옵니다

여기까지 오면 끝입니다. 이제

- 루틴 묶음 설정에서 시각과 알림(정시 / 5분 전 …)을 정하면 매일 그 시각에 알림이 옵니다
- 일정에 알림을 걸면 그 시각에, 하루 전으로 걸면 전날 그 시각에 옵니다
- 목표 D-day와 단계 마감일은 아침 9시에 알려줍니다
- 루틴이나 일정을 고치면 몇 초 뒤 서버에 자동으로 다시 알려줍니다. 따로 누를 게 없습니다
- 캘린더로 내보내는 일은 이제 안 해도 됩니다 (원하면 계속 쓸 수 있습니다)

---

## 안 될 때

**테스트 알림이 안 온다**
- 홈 화면 아이콘으로 연 앱인지 확인 (사파리 탭에서는 안 됩니다)
- 아이폰 설정 → 알림 → 하루 → 알림 허용이 켜져 있는지
- 아이폰 iOS 16.4 이상인지 (설정 → 일반 → 정보)
- 서버 상태 확인 버튼을 눌러 `ok` 인지
- 집중 모드가 켜져 있으면 알림이 조용히 들어옵니다

**테스트는 오는데 정해진 시각에 안 온다**
- 루틴 묶음에 **시각**이 있고 **알림**이 "알림 없음"이 아닌지 확인
- 6-3의 Cron Trigger 가 `* * * * *` 로 저장됐는지 확인
- 알림 설정을 바꾼 뒤 4초쯤 기다리면 서버로 전송됩니다. 앱을 끄지 않고 잠깐 기다려 보세요
- 알림 시각에 폰이 인터넷에 연결돼 있어야 합니다

**해외에 나갔더니 시각이 어긋난다**
- 폰 알림 설정에서 **주소 저장하고 연결하기** 를 한 번 더 누르면 현재 시간대로 갱신됩니다

**폰을 새로 사거나 앱을 지웠다가 다시 넣었다**
- 통계 탭에서 백업 불러오기로 기록을 옮기고, 폰 알림 설정에서 다시 연결하세요

---

## 나중에 앱을 고칠 때

`index.html` 만 고치면 됩니다. 고친 뒤

1. `sw.js` 맨 위의 `const VERSION = 'haru-v4'` 를 `haru-v5` 처럼 올리고
2. 1단계와 같은 방법으로 파일을 다시 올립니다 (Netlify 는 같은 자리에 다시 끌어다 놓기, GitHub 는 같은 저장소에 다시 업로드)

버전 숫자를 올리지 않으면 아이폰이 예전 화면을 계속 보여줄 수 있습니다.

## 요금

- Netlify / GitHub Pages: 개인 사이트 무료
- 클라우드플레어 워커: 하루 10만 번 실행까지 무료. 1분에 한 번이면 하루 1,440번이라 넉넉합니다
- KV: 하루 10만 번 읽기 무료
